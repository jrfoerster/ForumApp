@model ForumApp.Models.ThreadEdit

@{
    string _title = "Delete Forum";
    ViewBag.Title = _title;
}

<h1>@_title</h1>
<p class="text-danger">Are you sure you want to delete this?</p>

<div class="row mb-3">
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ForumName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ForumName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ThreadTitle)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ThreadTitle)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.ForumId)
        @Html.HiddenFor(model => model.ThreadId)

        <div class="row mb-3">
            <div class="btn-group btn-group-lg">
                @Html.ActionLink("Cancel", "Details", "Forum", new { id = Model.ForumId }, new { @class = "btn btn-light" })
                <button type="submit" class="btn btn-danger">Delete</button>
            </div>
        </div>
    }
</div>
