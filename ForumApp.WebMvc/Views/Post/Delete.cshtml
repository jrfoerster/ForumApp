@model ForumApp.Models.PostEdit

@{
    string _title = "Delete Post";
    ViewBag.Title = _title;
}

<h1>@_title</h1>
<p class="text-danger">Are you sure you want to delete this?</p>

<div class="row mb-3">
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Content)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Content)
        </dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.ThreadId)
        @Html.HiddenFor(model => model.PostId)

        <div class="row mb-3">
            <div class="btn-group btn-group-lg">
                @Html.ActionLink("Cancel", "Details", "Thread", new { id = Model.ThreadId }, new { @class = "btn btn-light" })
                <button type="submit" class="btn btn-danger">Delete</button>
            </div>
        </div>
    }
</div>
